---
- name: Bootstrap Kubernetes prerequisites for all tenants
  hosts: localhost
  gather_facts: false
  vars_files:
    - ../group_vars/all.yml
  vars:
    k8s_context: "{{ kube_context | default(omit) }}"

  roles:
    - role: bootstrap_k8s